# MIDI functionality
python-rtmidi>=1.5.0

# HID device support (equivalent to node-hid)
# Install ONE of the following HID packages:
# Option 1 (recommended): pip install hid
# Option 2: pip install pyhidapi  
# Option 3: pip install hidapi
# The code will automatically detect which one is available
hid>=1.0.0

# WebSocket server support (equivalent to ws)
websockets>=11.0.0

# Additional utilities
typing-extensions>=4.0.0
